## Infra Running

***Make sure you are logged into awscli on your machine***

1. Run the init command
    ```bash
        terraform init
    ```
2. Run the apply command
    ```bash
        terraform apply
    ```